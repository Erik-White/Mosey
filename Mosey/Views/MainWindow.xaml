<Window x:Class="Mosey.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mosey"
        xmlns:Views="clr-namespace:Mosey.Views"
        xmlns:ViewModels="clr-namespace:Mosey.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        >
    <Window.DataContext>
        <ViewModels:MainViewModel />
    </Window.DataContext>
    <Border BorderThickness="20" BorderBrush="White" Background="WhiteSmoke">
        <DockPanel MinHeight="100" HorizontalAlignment="Stretch" VerticalAlignment="Center" LastChildFill="False">
            <TextBlock Text="RepetitionsCount:" />
            <TextBlock Text="{Binding Path=ScanRepetitionsCount}" />
            <Button Command="{Binding Path=StartScanCommand}" Content="Start scan" />
            <Button Command="{Binding Path=StopScanCommand}" Content="Stop scan" />
        </DockPanel>
    </Border>
</Window>
