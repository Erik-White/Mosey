<Controls:MetroWindow
        x:Class="Mosey.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mosey"
        xmlns:Views="clr-namespace:Mosey.Views"
        xmlns:ViewModels="clr-namespace:Mosey.ViewModels"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Mosey"
        WindowStartupLocation="CenterScreen"
        MinHeight="600"
        MinWidth="800"
        BorderThickness="0"
        GlowBrush="{DynamicResource AccentColorBrush}"
        >
    <Window.DataContext>
        <ViewModels:MainViewModel />
    </Window.DataContext>
    <Border BorderThickness="20" BorderBrush="White" Background="WhiteSmoke">
        <DockPanel MinHeight="100" HorizontalAlignment="Stretch" VerticalAlignment="Center" LastChildFill="False">
            <TextBlock Text="RepetitionsCount:" />
            <TextBlock Text="{Binding Path=ScanRepetitionsCount}" />
            <Label Content="Scan lag interval time (seconds):" />
            <Controls:NumericUpDown Value="{Binding Path=ScanInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Scan lag repetitions:" />
            <Controls:NumericUpDown Value="{Binding Path=ScanRepetitions, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="{Binding Path=ScanRepetitions}" />
            <Button Command="{Binding Path=StartScanCommand}" Content="Start scan" />
            <Button Command="{Binding Path=PauseScanCommand}" Content="Pause scan" />
            <Button Command="{Binding Path=ResumeScanCommand}" Content="Resume scan" />
            <Button Command="{Binding Path=StopScanCommand}" Content="Stop scan" />
            <Controls:ProgressRing IsActive="{Binding Path=IsScanRunning}" />
        </DockPanel>
    </Border>
</Controls:MetroWindow>
