<Controls:MetroWindow
        x:Class="Mosey.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        xmlns:local="clr-namespace:Mosey"
        xmlns:Views="clr-namespace:Mosey.Views"
        xmlns:ViewModels="clr-namespace:Mosey.ViewModels"
        xmlns:Converters="clr-namespace:Mosey.ViewModels.Converters"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:CustomControls="clr-namespace:Mosey.Models.Controls"
        xmlns:IconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="Mosey"
        WindowStartupLocation="CenterScreen"
        Height="600"
        Width="800"
        MinHeight="400"
        MinWidth="600"
        Style="{StaticResource StyleBase}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        >
    <!--
    Data context is set at window instantiation
    <Window.DataContext>
        <ViewModels:MainViewModel />
    </Window.DataContext>
    -->
    <Window.Resources>
        <CollectionViewSource x:Key="ScannerDevicesOrdered" Source="{Binding Path=ScannerDevices}">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="ID" Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
        <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" FalseValue="Collapsed" />
        <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityHiddenConverter" FalseValue="Hidden" />
        <Converters:ValueConverterGroup x:Key="BoolToInvertedVisibilityConverter">
            <Converters:BoolToInvertedBoolConverter />
            <Converters:BoolToVisibilityConverter FalseValue="Collapsed" />
        </Converters:ValueConverterGroup>
        <Thickness x:Key="VerticalMargin">0 15 0 15</Thickness>
        <Thickness x:Key="HorizontalMargin">15 0 15 0</Thickness>
        <Thickness x:Key="LabelMargin">0 5 0 0</Thickness>
    </Window.Resources>

    <Border BorderThickness="15" BorderBrush="White" Background="WhiteSmoke">
        <DockPanel MinHeight="100" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <!--#region Header and logo -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                <Label Style="{StaticResource IconLarge}" Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToInvertedVisibilityConverter}}">
                    <IconPacks:Material Kind="DotsHorizontalCircle" Height="60" Width="60" />
                </Label>
                <Label Style="{StaticResource IconLarge}" Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToVisibilityConverter}}">
                    <!-- The spin cannot be set with binding as it is not a DependencyProperty. The same issue affects Height and Width, which must be set manually -->
                    <IconPacks:Material Kind="DotsHorizontalCircle" Height="60" Width="60" Spin="True" SpinDuration="3.5" />
                </Label>
                <TextBlock Text="MOSEY" Style="{StaticResource TextBlockHeading}" Margin="0" />
            </StackPanel>
            <!--#endregion Header and logo -->
            <!--#region Sequence controls -->
            <Grid DockPanel.Dock="Top" Margin="{StaticResource VerticalMargin}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="150" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition MinHeight="30" />
                    <RowDefinition MinHeight="30" />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <Style x:Key="ToggleButtonMain" TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource ToggleButtonHorizontal}">
                        <Style.Resources>
                            <Style TargetType="FrameworkElement">
                                <Setter Property="DockPanel.Dock" Value="Left" />
                            </Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockHeading}">
                                <Style.Setters>
                                    <Setter Property="Margin" Value="0" />
                                    <Setter Property="CustomControls:Margin.Bottom" Value="0" />
                                    <Setter Property="Foreground" Value="{StaticResource MahApps.Brushes.AccentSelectedColor}" />
                                    <Setter Property="TextWrapping" Value="Wrap" />
                                </Style.Setters>
                            </Style>
                            <Style TargetType="Label" BasedOn="{StaticResource IconNormal}">
                                <Setter Property="Foreground" Value="{StaticResource MahApps.Brushes.AccentSelectedColor}" />
                            </Style>
                        </Style.Resources>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard Storyboard.TargetProperty="Background.(SolidColorBrush.Opacity)" AutoReverse="True">
                                            <DoubleAnimation To="0.30" Duration="00:00:00.40" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Setter Property="Cursor" Value="Wait" />
                            </Trigger>
                        </Style.Triggers>
                        <Style.Setters>
                            <!-- Need to override the default ControlTemplate in order to remove the default triggers -->
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background" Value="{StaticResource MahApps.Brushes.Accent}" />
                            <Setter Property="Foreground" Value="{StaticResource MahApps.Brushes.AccentSelectedColor}" />
                        </Style.Setters>
                    </Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockNormal}">
                        <Setter Property="Padding" Value="0" />
                    </Style>
                    <Style TargetType="Controls:NumericUpDown" BasedOn="{StaticResource NumericUpDownNormal}">
                        <Setter Property="IsEnabled" Value="{Binding IsScanRunning, Converter={Converters:BoolToInvertedBoolConverter}}" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                    </Style>
                </Grid.Resources>
                <!-- Scan options -->
                <TextBlock Text="Interval time (minutes)" Grid.Column="0" Grid.Row="0" />
                <Controls:NumericUpDown Value="{Binding Path=ScanInterval, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Grid.Row="0" />
                <TextBlock Text="Total repetitions" Grid.Column="2" Grid.Row="0" HorizontalAlignment="Right" />
                <Controls:NumericUpDown Value="{Binding Path=ScanRepetitions, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Column="3" Grid.Row="0" />
                <TextBox Text="Save location" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" />
                <TextBox Text="{Binding Path=ImageSavePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Cursor="IBeam" IsEnabled="{Binding IsScanRunning, Converter={Converters:BoolToInvertedBoolConverter}}" Padding="5" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3" />
                <Button Command="{Binding SelectFolderCommand}" IsEnabled="{Binding IsScanRunning, Converter={Converters:BoolToInvertedBoolConverter}}" Panel.ZIndex="100" HorizontalAlignment="Right" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3">
                    <Label Style="{StaticResource IconSmall}" Margin="{StaticResource HorizontalMargin}">
                        <IconPacks:Material Kind="DotsHorizontal" Width="20" Height="20" />
                    </Label>
                </Button>
                <TextBox IsEnabled="{Binding IsScanRunning, Converter={Converters:BoolToInvertedBoolConverter}}" Padding="5" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3" />
                <!--#region Start/stop scan button -->
                <ToggleButton Command="{Binding Path=StartStopScanCommand, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ToggleButtonMain}" Grid.Column="4" Grid.RowSpan="2">
                    <!--
                        A DockPanel is used instead of a stackpanel otherwise child elements
                        have infinite width and will not wrap their contents
                    -->
                    <DockPanel>
                        <Label Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToInvertedVisibilityConverter}}">
                            <IconPacks:Material Kind="PlayCircle" Height="40" Width="40" />
                        </Label>
                        <TextBlock Text="Start scanning" Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToInvertedVisibilityConverter}}" />
                        <Label Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToVisibilityConverter}}">
                            <IconPacks:Material Kind="CloseCircle" Height="40" Width="40" />
                        </Label>
                        <TextBlock Text="Stop scanning" Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </DockPanel>
                </ToggleButton>
                <!--
                <ToggleButton Command="{Binding Path=StartStopScanCommand}" Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToInvertedVisibilityConverter}}" IsEnabled="{Binding IsScanRunning, Converter={Converters:BoolToInvertedBoolConverter}}" Grid.Column="4" Grid.RowSpan="2">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton" BasedOn="{StaticResource ToggleButtonMain}">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard Name="HighlightAnim">
                                            <Storyboard TargetProperty="Opacity" AutoReverse="True">
                                                <DoubleAnimation From="1" To="0.95" Duration="00:00:02"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <StopStoryboard BeginStoryboardName="HighlightAnim"/>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>
                    <StackPanel Orientation="Horizontal">
                        <Label Style="{StaticResource IconNormal}">
                            <IconPacks:Material Kind="PlayCircle" Height="40" Width="40" />
                        </Label>
                        <TextBlock Text="Start scan" />
                    </StackPanel>
                </ToggleButton>
                <ToggleButton Command="{Binding Path=StopScanCommand}" Style="{StaticResource ToggleButtonMain}" Visibility="{Binding Path=IsScanRunning, Converter={StaticResource BoolToVisibilityConverter}}" IsEnabled="True" Grid.Column="4" Grid.RowSpan="2">
                    <StackPanel Orientation="Horizontal">
                        <Label Style="{StaticResource IconNormal}">
                            <IconPacks:Material Kind="CloseCircle" Height="40" Width="40" />
                        </Label>
                        <TextBlock Text="Stop scan" />
                    </StackPanel>
                </ToggleButton>
                -->
                <!--#endregion Start/stop scan button -->
                <!-- Advanced options -->
                <!--
                <Label Content="Image format" Grid.Column="1" Grid.Row="2" Margin="{StaticResource HorizontalMargin}" />
                <Controls:DropDownList  IsEnabled="{Binding IsScanRunning, Converter={Converters:BoolToInvertedBoolConverter}}" Margin="{StaticResource HorizontalMargin}" Padding="5" Grid.Column="1" Grid.Row="3" />
                -->
            </Grid>
            <!--#endregion Sequence controls -->
            <!--#region Progress information -->
            <Border DockPanel.Dock="Top" IsEnabled="{Binding Path=IsScanRunning}" Style="{StaticResource VisibleExpandYBounceAnimation}" Padding="{StaticResource HorizontalMargin}" Background="{StaticResource MahApps.Brushes.Gray1}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="150" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition MinHeight="20" />
                        <RowDefinition MinHeight="80" />
                        <RowDefinition MinHeight="20" />
                    </Grid.RowDefinitions>
                    <Grid.Resources>
                        <Style TargetType="Label">
                            <Style.Setters>
                                <Setter Property="Foreground" Value="{StaticResource MahApps.Brushes.Gray5}" />
                            </Style.Setters>
                        </Style>
                    </Grid.Resources>
                    <!-- Scans completed -->
                    <Label Content="Completed" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" HorizontalContentAlignment="Center" />
                    <Label Content="{Binding ScanRepetitionsCount, FallbackValue='0'}" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" FontSize="60" />
                    <!-- Next scan -->
                    <Label Content="Next scan" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="2" Grid.Row="0" Grid.ColumnSpan="2" HorizontalContentAlignment="Center" />
                    <Label Content="{Binding ScanNextTime, FallbackValue=''}" ContentStringFormat="mm':'ss" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" FontSize="60" />
                    <!-- Finish time -->
                    <Label Content="Finish time" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="4" Grid.Row="0" Grid.ColumnSpan="2" HorizontalContentAlignment="Center" />
                    <Label Content="{Binding ScanFinishTime}" ContentStringFormat="HH:mm" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="4" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" FontSize="60" />
                    <Label Content="{Binding ScanFinishTime}" ContentStringFormat="dddd dd" Visibility="{Binding IsScanRunning, Converter={StaticResource BoolToVisibilityConverter}}" Margin="{StaticResource HorizontalMargin}" Padding="0" Grid.Column="4" Grid.Row="2" Grid.ColumnSpan="2" HorizontalContentAlignment="Center" />
                    <!--#endregion Progress information -->
                </Grid>
            </Border>
            <StackPanel DockPanel.Dock="Top" Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <Button Command="{Binding Path=RefreshScannersCommand}" Content="Refresh scanners" />
                    <Button Command="{Binding Path=EnableScannersCommand}" Content="Enable all scanners" />
                    <Button Command="{Binding Path=ManualScanCommand}" Content="Manual scan" />
                </StackPanel>
            </StackPanel>
            <!--#region Display scanners and their properties -->
            <ScrollViewer DockPanel.Dock="Top" Visibility="{Binding ScannerDevices.IsEmpty, Converter={StaticResource BoolToInvertedVisibilityConverter}}" VerticalScrollBarVisibility="Visible" VerticalAlignment="Stretch">
                <ItemsControl ItemsSource="{Binding Source={StaticResource ScannerDevicesOrdered}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid VerticalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition MinHeight="60" />
                                    <RowDefinition MinHeight="40" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Name}" Grid.Column="0" Grid.ColumnSpan="4" />
                                <TextBlock Text="{Binding ID}" Grid.Column="0" Grid.Row="1" />
                                <TextBlock Text="Connected" Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}" Grid.Column="1" Grid.Row="1" />
                                <Controls:ToggleSwitchButton Content="Enable scanning" IsChecked="{Binding Path=IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsImaging, Converter={Converters:BoolToInvertedBoolConverter}}" Cursor="Hand" Grid.Column="2" Grid.Row="1" />
                                <Label Visibility="{Binding IsImaging, Converter={StaticResource BoolToVisibilityConverter}}" Style="{StaticResource IconNormal}" Grid.Column="4" Grid.RowSpan="2">
                                    <IconPacks:Material Kind="DotsHorizontalCircle" Spin="True" SpinDuration="3.5" Height="40" Width="40" />
                                </Label>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <!-- Display a message if no scanners found -->
            <StackPanel DockPanel.Dock="Top" Visibility="{Binding ScannerDevices.IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}" HorizontalAlignment="Center">
                <TextBlock Text="Waiting for scanners" Style="{StaticResource TextBlockHeading}" />
                <Label Style="{StaticResource IconExtraLarge}">
                    <IconPacks:Material Kind="Brightness5" Spin="True" SpinDuration="2" Height="80" Width="80" />
                </Label>
            </StackPanel>
            <!--#endregion Display scanners and their properties -->
        </DockPanel>
    </Border>
</Controls:MetroWindow>